{
    "name": "study-group-match",
    "version": "1.0.0",
    "scripts": {
        "start": "cd backend && npm start",
        "build": "cd frontend && npm install && npm run build",
        "render-start": "npm run build && cd backend && npm start"
    },
    "devDependencies": {
        "nodemon": "^3.1.7",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "express-mysql-session": "^3.0.3"
    }
}
